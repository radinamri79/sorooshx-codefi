# Next.js Configuration
# Copy this file to .env.local for local development
# Copy to .env.production.local for production deployment
# And fill in the appropriate values for each environment

# API Base URL — points to your Django backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Site URL — used for meta tags, links, and canonical URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3000
